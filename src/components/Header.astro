---
import { useTranslations } from "../i18n/utils";

// Determine language from URL path
const isEnglish = Astro.url.pathname.startsWith("/en");
const lang = isEnglish ? "en" : "pl";
const t = useTranslations(lang);

const currentPath = Astro.url.pathname;

// Helper to build URLs based on current language
const getUrl = (path: string) => {
    if (lang === "en") {
        return path === "/" ? "/en/" : `/en${path}`;
    }
    return path;
};
---

<header class="navbar bg-base-100 shadow-lg sticky top-0 z-50">
    <div class="navbar-start">
        <div class="dropdown">
            <div tabindex="0" role="button" class="btn btn-ghost lg:hidden">
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    class="h-5 w-5"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke="currentColor"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M4 6h16M4 12h8m-8 6h16"></path>
                </svg>
            </div>
            <ul
                tabindex="0"
                class="menu menu-sm dropdown-content mt-3 z-[1] p-2 shadow bg-base-100 rounded-box w-52"
            >
                <li>
                    <a
                        href={getUrl("/")}
                        class={currentPath === getUrl("/") ? "active" : ""}
                        >{t("nav.home")}</a
                    >
                </li>
                <li>
                    <a
                        href={getUrl("/gallery")}
                        class={currentPath.includes("/gallery") ? "active" : ""}
                        >{t("nav.gallery")}</a
                    >
                </li>
                <li>
                    <a
                        href={getUrl("/funding")}
                        class={currentPath.includes("/funding") ? "active" : ""}
                        >{t("nav.funding")}</a
                    >
                </li>
                <li>
                    <a
                        href={getUrl("/contact")}
                        class={currentPath.includes("/contact") ? "active" : ""}
                        >{t("nav.contact")}</a
                    >
                </li>
            </ul>
        </div>
        <a href={getUrl("/")} class="btn btn-ghost normal-case">
            <!-- Inline SVG logo with currentColor for theme adaptation -->
            <svg
                width="120"
                height="40"
                viewBox="0 0 120 40"
                fill="none"
                xmlns="http://www.w3.org/2000/svg"
                class="h-10 w-auto"
            >
                <!-- Background circle/badge -->
                <circle cx="20" cy="20" r="18" fill="currentColor" opacity="0.1"
                ></circle>

                <!-- Welding spark/gear icon -->
                <g transform="translate(8, 8)">
                    <!-- Gear/Industrial symbol -->
                    <path
                        d="M12 2L13.5 6.5L18 8L13.5 9.5L12 14L10.5 9.5L6 8L10.5 6.5L12 2Z"
                        fill="currentColor"></path>
                    <path
                        d="M12 10L13 12.5L15.5 13.5L13 14.5L12 17L11 14.5L8.5 13.5L11 12.5L12 10Z"
                        fill="currentColor"
                        opacity="0.7"></path>

                    <!-- Welding arc effect -->
                    <circle
                        cx="12"
                        cy="12"
                        r="8"
                        stroke="currentColor"
                        stroke-width="1.5"
                        fill="none"
                        opacity="0.3"></circle>
                    <circle
                        cx="12"
                        cy="12"
                        r="10"
                        stroke="currentColor"
                        stroke-width="0.5"
                        fill="none"
                        opacity="0.2"></circle>
                </g>

                <!-- TEI Text -->
                <text
                    x="45"
                    y="27"
                    font-family="system-ui, -apple-system, sans-serif"
                    font-size="22"
                    font-weight="700"
                    fill="currentColor"
                >
                    TEI
                </text>

                <!-- Subtle underline accent -->
                <rect
                    x="45"
                    y="30"
                    width="30"
                    height="2"
                    fill="currentColor"
                    opacity="0.6"
                    rx="1"></rect>
            </svg>
        </a>
    </div>

    <div class="navbar-center hidden lg:flex">
        <ul class="menu menu-horizontal px-1 gap-4 text-lg">
            <li>
                <a
                    href={getUrl("/")}
                    class={currentPath === getUrl("/") ? "active" : ""}
                    >{t("nav.home")}</a
                >
            </li>
            <li>
                <a
                    href={getUrl("/gallery")}
                    class={currentPath.includes("/gallery") ? "active" : ""}
                    >{t("nav.gallery")}</a
                >
            </li>
            <li>
                <a
                    href={getUrl("/funding")}
                    class={currentPath.includes("/funding") ? "active" : ""}
                    >{t("nav.funding")}</a
                >
            </li>
            <li>
                <a
                    href={getUrl("/contact")}
                    class={currentPath.includes("/contact") ? "active" : ""}
                    >{t("nav.contact")}</a
                >
            </li>
        </ul>
    </div>

    <div class="navbar-end gap-2">
        <!-- Language Switcher -->
        <div class="dropdown dropdown-end">
            <div
                tabindex="0"
                role="button"
                class="btn btn-ghost btn-circle"
                aria-label={t("language.toggle")}
            >
                <span class="text-xl" id="current-lang-flag"
                    >{lang === "pl" ? "🇵🇱" : "🇬🇧"}</span
                >
            </div>
            <ul
                tabindex="0"
                class="dropdown-content z-[1] menu p-2 shadow bg-base-100 rounded-box w-32 mt-4"
            >
                <li>
                    <button class="language-switch" data-lang="pl">
                        <span class="text-xl">🇵🇱</span> Polski
                    </button>
                </li>
                <li>
                    <button class="language-switch" data-lang="en">
                        <span class="text-xl">🇬🇧</span> English
                    </button>
                </li>
            </ul>
        </div>

        <!-- Theme Switcher -->
        <label
            class="swap swap-rotate btn btn-ghost btn-circle"
            aria-label={t("theme.toggle")}
        >
            <input type="checkbox" class="theme-controller" value="dark" />

            <!-- sun icon -->
            <svg
                class="swap-off fill-current w-6 h-6"
                xmlns="http://www.w3.org/2000/svg"
                viewBox="0 0 24 24"
            >
                <path
                    d="M5.64,17l-.71.71a1,1,0,0,0,0,1.41,1,1,0,0,0,1.41,0l.71-.71A1,1,0,0,0,5.64,17ZM5,12a1,1,0,0,0-1-1H3a1,1,0,0,0,0,2H4A1,1,0,0,0,5,12Zm7-7a1,1,0,0,0,1-1V3a1,1,0,0,0-2,0V4A1,1,0,0,0,12,5ZM5.64,7.05a1,1,0,0,0,.7.29,1,1,0,0,0,.71-.29,1,1,0,0,0,0-1.41l-.71-.71A1,1,0,0,0,4.93,6.34Zm12,.29a1,1,0,0,0,.7-.29l.71-.71a1,1,0,1,0-1.41-1.41L17,5.64a1,1,0,0,0,0,1.41A1,1,0,0,0,17.66,7.34ZM21,11H20a1,1,0,0,0,0,2h1a1,1,0,0,0,0-2Zm-9,8a1,1,0,0,0-1,1v1a1,1,0,0,0,2,0V20A1,1,0,0,0,12,19ZM18.36,17A1,1,0,0,0,17,18.36l.71.71a1,1,0,0,0,1.41,0,1,1,0,0,0,0-1.41ZM12,6.5A5.5,5.5,0,1,0,17.5,12,5.51,5.51,0,0,0,12,6.5Zm0,9A3.5,3.5,0,1,1,15.5,12,3.5,3.5,0,0,1,12,15.5Z"
                ></path>
            </svg>

            <!-- moon icon -->
            <svg
                class="swap-on fill-current w-6 h-6"
                xmlns="http://www.w3.org/2000/svg"
                viewBox="0 0 24 24"
            >
                <path
                    d="M21.64,13a1,1,0,0,0-1.05-.14,8.05,8.05,0,0,1-3.37.73A8.15,8.15,0,0,1,9.08,5.49a8.59,8.59,0,0,1,.25-2A1,1,0,0,0,8,2.36,10.14,10.14,0,1,0,22,14.05,1,1,0,0,0,21.64,13Zm-9.5,6.69A8.14,8.14,0,0,1,7.08,5.22v.27A10.15,10.15,0,0,0,17.22,15.63a9.79,9.79,0,0,0,2.1-.22A8.11,8.11,0,0,1,12.14,19.73Z"
                ></path>
            </svg>
        </label>
    </div>
</header>

<script>
    // Theme switcher logic
    function initTheme() {
        const theme = localStorage.getItem("theme") || "light";
        document.documentElement.setAttribute("data-theme", theme);

        const themeController = document.querySelector(
            ".theme-controller",
        ) as HTMLInputElement;
        if (themeController) {
            themeController.checked = theme === "dark";
        }
    }

    function setupThemeToggle() {
        const themeController = document.querySelector(
            ".theme-controller",
        ) as HTMLInputElement;

        if (themeController) {
            themeController.addEventListener("change", (e) => {
                const target = e.target as HTMLInputElement;
                const newTheme = target.checked ? "dark" : "light";
                document.documentElement.setAttribute("data-theme", newTheme);
                localStorage.setItem("theme", newTheme);
            });
        }
    }

    // Language switcher logic
    function setupLanguageSwitcher() {
        const languageButtons = document.querySelectorAll(".language-switch");

        languageButtons.forEach((button) => {
            button.addEventListener("click", (e) => {
                const target = e.currentTarget as HTMLElement;
                const newLang = target.getAttribute("data-lang") as "pl" | "en";

                if (newLang) {
                    // Store language preference
                    localStorage.setItem("language", newLang);

                    // Get current path and switch language
                    const currentPath = window.location.pathname;
                    const isEnglish = currentPath.startsWith("/en");
                    let newPath = "";

                    if (newLang === "en") {
                        // Switching to English
                        if (isEnglish) {
                            newPath = currentPath;
                        } else {
                            newPath =
                                currentPath === "/"
                                    ? "/en/"
                                    : `/en${currentPath}`;
                        }
                    } else {
                        // Switching to Polish
                        if (!isEnglish) {
                            newPath = currentPath;
                        } else {
                            newPath = currentPath.replace(/^\/en/, "") || "/";
                        }
                    }

                    window.location.href = newPath;
                }
            });
        });
    }

    // Initialize on page load
    initTheme();
    setupThemeToggle();
    setupLanguageSwitcher();

    // Re-initialize after navigation (for view transitions if enabled)
    document.addEventListener("astro:after-swap", () => {
        initTheme();
        setupThemeToggle();
        setupLanguageSwitcher();
    });
</script>

<style>
    .navbar {
        backdrop-filter: blur(10px);
    }
</style>
