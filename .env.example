# Deployment Configuration
# Copy this file to .env and fill in your actual values

# Remote server credentials for deployment
DEPLOY_USER=your_username
DEPLOY_HOST=your.server.ip.or.hostname
DEPLOY_PATH=~/path/to/deployment

# Contact Form Email
# The email address where contact form submissions will be sent
# Note: Astro requires PUBLIC_ prefix for variables accessible in components
PUBLIC_CONTACT_EMAIL=your-email@example.com
